export enum OfficeType {
    INFRASTRUCTURE = "infrastructure",
    ENVIRONMENT = "environment",
    SAFETY = "safety",
    SANITATION = "sanitation",
    TRANSPORT = "transport",
    OTHER = "other"
}